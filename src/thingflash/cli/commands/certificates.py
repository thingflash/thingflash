"""`thingflash certificates` command group (scaffold)."""

import typer

app = typer.Typer(no_args_is_help=True, help="Manage AWS IoT device certificates.")

# TODO: implement `create`, `list`, `revoke`, `rotate`.
